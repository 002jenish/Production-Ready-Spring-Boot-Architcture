version: '3.9'

# Docker Compose to run the ArchForge generator application itself

services:
  archforge:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: archforge-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
